import styled from "styled-components";

const Button = styled.button`
  outline: none;
  border: none;
  display: flex;
  gap: 11px;
  align-items: center;
  position: absolute;
  top: 29px;
  left: 94px;
  font-family: Jura;
  font-size: 14px;
  font-weight: 700;
  line-height: 17px;
  text-align: left;
  cursor: pointer;
`

export {
  Button
}